<!--pages/wxs/wxs.wxml-->
<!-- wxs与wxml结合，在wxml中直接使用函数，而不是通过监听调用js文件page里的函数 -->

<!-- 保留两位小数 -->
<!-- 错误写法 -->
<!-- <view>{{26.3345.toFixed(2)}}</view> -->
<!-- <view>{{toFixed(26.3345)}}</view> -->

<!-- 使用wxs定义函数 方式一：直接在wxml中写入-->
<!-- <wxs module='info'>
  // js代码
  var message = "hello world";
  var name = 'gcl';
  var sum = function(num1,num2){
    return num1+num2;
  }

  // 所定义的变量都在模块中使用，想在外部使用需要模块化导出
module.exports = {
  message:message,
  name:name,
  sum:sum,

}
</wxs> -->

<!-- 方式二：定义在单独的wxs文件中，再文件进行导入 -->
<wxs src="../../wxs/info.wxs" module="info"/>
<view>{{info.name}}</view>
<view>{{info.sum(1,2)}}</view>

<!-- wxs的使用，运算 -->
<wxs src="../../wxs/format.wxs" module="format"/>
<view>{{format.priceFormat(price)}}</view>